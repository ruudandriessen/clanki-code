groups:
  - name: "UI"
    description: "React frontend components and pages"
  - name: "API"
    description: "HTTP API handlers and worker middleware"

overrides:
  - pattern: "frontend/**"
    group: "UI"
  - pattern: "worker/**"
    group: "API"
